<!DOCTYPE html>
<html>
<head>
  <title>Centuries Past Trading Co. CRM</title>
  <%= stylesheet_link_tag    "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>


 <div class="container">
    <div class="logo"> <%= link_to image_tag("CPTCHeader3.png"),root_path%> </div>
    <div class="navbar navbar-default" rol="navigation">
      <% if get_current_user == nil %>
         <p><%= link_to 'Sign up', new_user_path %> <strong>|</strong> <%= link_to 'Sign in', sessions_path %></p>
        <% else %>
            <h4>Welcome, <%= get_current_user.name %> | <%= link_to 'Edit Profile', edit_user_path(get_current_user) %> | <%= link_to "Sign out", session_path(session), method: :delete, data: { confirm: 'Are you sure you want to sign out?' } %> </h4>
      <% end %>
    </div>
    <!--<div class="container panel panel-default">-->
    <% if get_current_user != nil %>
    <%= link_to 'New Customer', new_customer_path %> |  <%= link_to 'View All Customers', customers_path%> <% if get_current_user.admin? %>| <%= link_to 'View All Users', users_path%> <%end%>

    <br />
    <% end %>
    <!--</div>-->
    <div class="container">
      <%= yield %>
    </div>
  </div>
</body>
</html>
